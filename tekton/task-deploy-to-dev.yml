apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: deploy-to-dev
spec:
  inputs:
    params:
      - name: APP_NAME
        description: The name of the application
        default: 'infographic'
      - name: NAMESPACE_BUILD
        description: The name of the namespace build
        default: 'infographic-pipeline'
      - name: NAMESPACE_DEV
        description: The name of the namespace dev
        default: 'infographic-dev'
  steps:
    - name: 
      params:
        - name: SCRIPT
          value: oc $@
        - name: ARGS
          value:
            - tag $(inputs.params.NAMESPACE_BUILD)/$(inputs.params.APP_NAME):latest $(inputs.params.NAMESPACE_DEV)/$(inputs.params.APP_NAME):latest
      taskRef:
        kind: ClusterTask
        name: openshift-client

